# Copy to .env and fill in. .env is gitignored — never commit the real key.
#
# Verify the endpoint against docs.typesafe.ai BEFORE putting a real key here.
# Several sites ranking for Jev right now are SEO farms, and at least one
# publishes an endpoint on a domain unrelated to typesafe.ai.
JEV_API_KEY=
JEV_ENDPOINT=
